"import React, { useState } from 'react';\n\nconst ResultsPage = ({ title, thumbnailUrl, summary, onReset }) => {\n  const [copied, setCopied] = useState(false);\n\n  const handleCopy = () => {\n    if (!summary || summary.length === 0) return;\n    const textToCopy = summary.map(point => `• ${point}`).join('\\n');\n    navigator.clipboard.writeText(textToCopy).then(() => {\n      setCopied(true);\n      setTimeout(() => setCopied(false), 2000);\n    });\n  };\n\n  return (\n    <div className=\"stitch-results\">\n      <div className=\"stitch-video-section\">\n        <div className=\"stitch-hero-eyebrow\">LATEST PROCESSING</div>\n        <div className=\"stitch-results-header\">\n          <h1 className=\"stitch-results-title\">{title}</h1>\n        </div>\n        <div className=\"stitch-video-placeholder\">\n          {thumbnailUrl ? (\n            <img src={thumbnailUrl} alt={title} />\n          ) : (\n            <span style={{ color: 'var(--text-muted)' }}>Video Thumbnail</span>\n          )}\n        </div>\n        <div className=\"stitch-meta-row\">\n          <div className=\"stitch-meta-item\">\n            <span className=\"stitch-meta-label\">Duration</span>\n            <span className=\"stitch-meta-value\">--:--</span>\n          </div>\n          <div className=\"stitch-meta-item\">\n            <span className=\"stitch-meta-label\">Language</span>\n            <span className=\"stitch-meta-value\">English (US)</span>\n          </div>\n          <div className=\"stitch-meta-item\">\n            <span className=\"stitch-meta-label\">Source</span>\n            <span className=\"stitch-meta-value\">YouTube API</span>\n          </div>\n        </div>\n      </div>\n\n      <div className=\"stitch-summary-card\">\n        <div className=\"stitch-summary-header\">\n          <span>EXECUTIVE SUMMARY</span>\n          <button className=\"stitch-copy-btn\" onClick={handleCopy}>\n            {copied ? 'COPIED' : 'COPY'}\n          </button>\n        </div>\n        \n        <div className=\"stitch-su
<truncated 626 bytes>